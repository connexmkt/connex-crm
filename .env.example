# Supabase do próprio connex-crm (autenticação, dados do CRM)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Postgres do próprio connex-crm (usado pelo Prisma; sem models no momento).
DATABASE_URL=
DIRECT_URL=
